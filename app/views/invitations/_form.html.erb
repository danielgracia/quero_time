<%= form_with(model: invitation, local: true) do |form| %>
  <% if invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
      <% invitation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :invitation_state %>
  </div>

  <div class="field">
    <%= form.label :users_id %>
    <%= form.text_field :users_id, id: :invitation_users_id %>
  </div>

  <div class="field">
    <%= form.label :teams_id %>
    <%= form.text_field :teams_id, id: :invitation_teams_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
