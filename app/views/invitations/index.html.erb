<p id="notice"><%= notice %></p>

<h1>Invitations</h1>

<table>
  <thead>
    <tr>
      <th>State</th>
      <th>Users</th>
      <th>Teams</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @invitations.each do |invitation| %>
      <tr>
        <td><%= invitation.state %></td>
        <td><%= invitation.user %></td>
        <td><%= invitation.team %></td>
        <td><%= link_to 'Exibir', invitation, class: "btn btn-default" %></td>
        <td><%= link_to 'Editar', edit_invitation_path(invitation), class: "btn btn-default" %></td>
        <td><%= link_to 'Remover', invitation, method: :delete, data: { confirm: 'Voce tem certeza?' }, class: "btn btn-default" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
