<p id="notice"><%= notice %></p>

<h1>User Skills</h1>

<table>
  <thead>
    <tr>
      <th>Level</th>
      <th>Users</th>
      <th>Skills</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_skills.each do |user_skill| %>
      <tr>
        <td><%= user_skill.level %></td>
        <td><%= user_skill.users %></td>
        <td><%= user_skill.skills %></td>
        <td><%= link_to 'Show', user_skill %></td>
        <td><%= link_to 'Edit', edit_user_skill_path(user_skill) %></td>
        <td><%= link_to 'Destroy', user_skill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User Skill', new_user_skill_path %>
