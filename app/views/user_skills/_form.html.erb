<%= form_with(model: user_skill, local: true) do |form| %>
  <% if user_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_skill.errors.count, "error") %> prohibited this user_skill from being saved:</h2>

      <ul>
      <% user_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    FIXME: este campo será hidden 
    <%= form.label :users_id %>
    <%= form.text_field :users_id, id: :user_skill_users_id, class: "form-control"  %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.select :skills_id, SkillLevel.all.map{ |c| [c.name, c.id]}, {}, class: "form-control"  %>
  </div>

  <div class="field">
    <%= form.label :skills_id %>
    <%= form.select :skills_id, Skill.all.map{ |c| [c.name, c.id]}, {}, class: "form-control"  %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
